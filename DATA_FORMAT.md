# 跨端数据格式规范

本规范定义七个习惯教练产出的"反思卡片"数据格式，适用于三端：
- **coach-web**（网页版 AI 教练）
- **habit-app**（React Native App）
- **未来小程序**（Taro / 微信原生）

## 1. 反思卡片（Reflection Card）

每次教练对话产出一张卡片。一张卡片 = 一个 `.md` 文件。

### 1.1 文件命名

```
{YYYY-MM-DD}-{8位随机id}.md
```

示例：`2026-06-07-a1b2c3d4.md`

### 1.2 完整格式

```markdown
---
date: 2026-06-07T14:30:00+08:00
habit: habit1
tags: [被动感, 影响圈]
version: 1
---

## 镜子
你今天说了 3 次"没办法"——每一次都是把主动权交出去。

## 行动
明天 9 点开会时，主动提一个你自己的想法（而非等待被分配）。

## 反思
做完后问自己：主动提建议的感觉，和等待被分配有什么不同？
```

### 1.3 Frontmatter 字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `date` | ISO 8601 字符串 | ✅ | 对话时间，含时区 |
| `habit` | enum: `habit1` ~ `habit7` | ✅ | 关联的七个习惯 |
| `tags` | string[] | ❌ | 关键词标签，用于搜索和分类 |
| `version` | number | ✅ | 格式版本号，当前为 1 |

### 1.4 习惯枚举

| 值 | 含义 |
|------|------|
| `habit1` | 积极主动 — 影响圈/关注圈、暂停按钮、主动语言 |
| `habit2` | 以终为始 — 使命宣言、葬礼想象、两次创造 |
| `habit3` | 要事第一 — 四象限、大石头、周计划、说"不" |
| `habit4` | 双赢思维 — 富足心态、情感账户、第三选择 |
| `habit5` | 知彼解己 — 同理心倾听、自传式回应诊断 |
| `habit6` | 统合综效 — 重视差异、创造性合作 |
| `habit7` | 不断更新 — 四个维度更新、螺旋上升 |

### 1.5 正文区块

| 区块 | Markdown 标题 | 必填 | 说明 |
|------|---------------|------|------|
| 镜子 | `## 镜子` | ✅ | 1-3 句模式观察，不评判、不贴标签 |
| 行动 | `## 行动` | ✅ | 一个影响圈内具体动作，≤30 分钟可完成 |
| 反思 | `## 反思` | ✅ | 一个反思问题或用户的回答 |

### 1.6 空字段处理

- 必填字段为空时写 `（未填写）`
- `tags` 为空时写 `[]`
- `habit` 不能为空，至少为 `habit1`

---

## 2. 存放路径

### 2.1 各端路径

| 端 | 路径 | 说明 |
|------|------|------|
| habit-app | `.habits/reflections/{date}-{id}.md` | 本地文件系统 |
| coach-web | Cloudflare KV（运行时）→ 导出到本地 `.md` | 网页无法直接写本地文件 |
| 小程序 | 微信云开发数据库（运行时）→ 导出到本地 `.md` | 小程序无法直接写本地文件 |

### 2.2 与 habit-app 现有 .habits 目录的兼容

```
.habits/
├── mission.md              # 使命宣言
├── quadrant/               # 四象限周数据
├── daily/                  # 每日复盘
├── config.json             # 用户配置
└── reflections/            # ← 本规范新增
    ├── 2026-06-07-a1b2c3d4.md
    ├── 2026-06-07-e5f6g7h8.md
    └── 2026-06-08-i9j0k1l2.md
```

habit-app 在启动时检查 `reflections/` 目录是否存在，不存在则创建。读取目录下所有 `.md` 文件，解析 frontmatter 渲染反思卡片列表。

---

## 3. 导出 / 导入

### 3.1 单条导出

网页版/小程序 → 用户点击"导出" → 浏览器下载一个 `.md` 文件，格式符合本规范。

### 3.2 批量导出

网页版/小程序 → 用户点击"导出全部" → 打包为 `.zip`，内含多个 `.md` 文件。

### 3.3 导入到 App

habit-app → 设置页 → "导入反思卡片" → 选择 `.md` 或 `.zip` 文件 → 解压写入 `.habits/reflections/`。

---

## 4. 版本兼容

### 4.1 版本号策略

- `version: 1` — 当前版本（三区块：镜子/行动/反思 + 6 字段 frontmatter）
- 将来增加字段时递增版本号，解析时按版本号做兼容处理

### 4.2 解析器要求

解析器必须：
1. 使用标准 YAML frontmatter 解析（gray-matter 或等效库），禁止手写正则
2. 对未知版本号降级解析（尝试读取已知字段，未知字段忽略）
3. 对缺失必填字段填充默认值，不抛异常

---

## 5. 示例文件

### 示例 1：习惯一 — 被动感

```markdown
---
date: 2026-06-07T10:15:00+08:00
habit: habit1
tags: [被动感, 工作, 影响圈]
version: 1
---

## 镜子
你今天描述了 3 个"不得不做"的任务——但每个任务其实都是你选择的。
你把主动权外包给了别人。

## 行动
今天下午 2 点，花 15 分钟列出你真正能控制的 3 件事，选一件做。

## 反思
做完后：当你把注意力从"别人怎么这样"转向"我能做什么"，感觉有什么不同？
```

### 示例 2：习惯三 — 忙而不效

```markdown
---
date: 2026-06-07T20:00:00+08:00
habit: habit3
tags: [忙乱, 时间管理, 四象限]
version: 1
---

## 镜子
你的日程表被别人的紧急事填满了，第二象限（重要但不紧急）没有获得任何时间。

## 行动
本周五前，在日历上锁定 2 小时"大石头时间"，拒绝所有会议邀请。

## 反思
一周后回看：这 2 小时为你推进了什么？如果每周都有这 2 小时呢？
```
